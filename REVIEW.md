# Чеклист ревью

Открой [CODEX.md](./CODEX.md) и этот список. Номера совпадают.

Комментарий: `кодекс 3, нет test.step`.

Назначенный PR, не любой. Автор не обязан чинить всё, что ты написал.

---

## Сначала гейт

Если нет — дальше можно не смотреть, напиши одно предложение.

- [ ] Тесты зелёные
- [ ] Автор прогнал линт (или в diff нет sleep / force / `.only`)
- [ ] В описании PR понятно, что изменилось

---

## По кодексу

- [ ] **1. Где лежит.** Сценарий в `e2e`, пользователь в `helpers`, экран в `pages`. Регистрация не в `ProfilePage` и не в `BookingPage`. Локаторы не в spec, если страница есть.
- [ ] **2. Вынос.** Дубль вынесен. Метод страницы — смысл (`saveName`). `expect` в тесте.
- [ ] **3. Каркас.** `describe` = фича, `test` = проверка. `beforeEach`: свой пользователь + страница.
- [ ] **4. Степы.** Каждый шаг и проверка — `test.step` с человеческим именем. В проверочном степе нет кликов. Страницу открыли (шаг или хук).
- [ ] **5. Имена.** «что проверяем», не «тест 1» и не «fill input».
- [ ] **6. Нарезка.** Связанное после одного Act не размазано на микротесты без нужды. И наоборот: нет простыни на три несвязанных мира.
- [ ] **7. Данные.** Свои `makeUser` на тест. Пароль можно хардкодить, имя и почту — нет.
- [ ] **8. Локаторы.** Сначала role / label / testid. Повторился якорь — в странице, не копипаста в spec.
- [ ] **9. Ожидания.** Нет timeout / force / `.only` / `pause`. Нет `expect` сразу после `fill` без сохранения и reload.
- [ ] **10. Гейт.** Зелёный, линт, описание — см. сверху.
- [ ] **11. API Arrange.** Регистрация и другой сетап идут через API, если их UI не является предметом теста.
- [ ] **12. Cleanup.** Созданные аккаунты удаляются через `finally` или гарантированный `afterEach`; contexts регистрируются для cleanup до первого падающего действия. Вместе с аккаунтами каскадом удаляются навыки, слоты и бронирования.

---

## Не придирайся

Кавычки, prettier, lockfile — пропусти.  
Другое имя метода, смысл тот же — ок.  
Скрины, моки, «почему не testid» — не требуй.  
`BookingPage` в hw11 — обязанность, не плюс.

---

## В своём hw11

```
Линт: 0
Прогон: зелёный
Что поправил: …

Ревью: PR #<номер> — <ссылка>
```